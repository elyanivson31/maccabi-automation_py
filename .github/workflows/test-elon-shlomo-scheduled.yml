name: Test Elon Shlomo - Every 5 Minutes

on:
  schedule:
    # Run every 5 minutes (GitHub Actions minimum interval)
    - cron: '*/5 * * * *'
  workflow_dispatch:  # Allow manual triggering

jobs:
  run-test-elon-shlomo:
    runs-on: ubuntu-latest
    timeout-minutes: 10  # Prevent hanging jobs
    permissions:
      actions: write  # Required to disable the workflow
      contents: read

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install Chrome
        uses: browser-actions/setup-chrome@v1
        with:
          chrome-version: stable

      - name: Verify Chrome installation
        run: |
          which google-chrome-stable
          google-chrome-stable --version

      - name: Install video recording dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y ffmpeg
          echo "✓ FFmpeg installed for video recording"

      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          echo "✓ All dependencies installed"

      - name: Set PYTHONPATH
        run: echo "PYTHONPATH=$(pwd)" >> $GITHUB_ENV

      - name: Verify encrypted test data exists
        run: |
          if [ ! -f "test_data.json.enc" ]; then
            echo "❌ Error: test_data.json.enc not found!"
            exit 1
          fi
          echo "✓ Encrypted test data file exists"

      - name: Verify encryption key is set
        env:
          TEST_DATA_ENCRYPTION_KEY: ${{ secrets.TEST_DATA_ENCRYPTION_KEY }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHANNEL_ID: ${{ secrets.TELEGRAM_CHANNEL_ID }}
        run: |
          if [ -z "$TEST_DATA_ENCRYPTION_KEY" ]; then
            echo "❌ Error: TEST_DATA_ENCRYPTION_KEY secret is not set!"
            exit 1
          fi
          echo "✓ Encryption key is configured"

          if [ -z "$TELEGRAM_BOT_TOKEN" ]; then
            echo "⚠️ Warning: TELEGRAM_BOT_TOKEN not set - notifications will fail"
          fi

          if [ -z "$TELEGRAM_CHANNEL_ID" ]; then
            echo "⚠️ Warning: TELEGRAM_CHANNEL_ID not set - notifications will fail"
          fi

      - name: Start video recording
        run: |
          mkdir -p videos
          # Start ffmpeg recording in the background
          # DISPLAY=:99 is the default Xvfb display used by GitHub Actions
          ffmpeg -video_size 1920x1080 -framerate 25 -f x11grab -i :99.0 \
            -codec:v libx264 -preset ultrafast -pix_fmt yuv420p \
            videos/test-recording-${{ github.run_number }}.mp4 \
            > /dev/null 2>&1 &
          echo $! > /tmp/ffmpeg_pid
          echo "🎥 Video recording started (PID: $(cat /tmp/ffmpeg_pid))"
          sleep 2  # Give ffmpeg time to initialize

      - name: Run test_elon_shlomo
        id: run_test
        continue-on-error: true
        env:
          TEST_DATA_ENCRYPTION_KEY: ${{ secrets.TEST_DATA_ENCRYPTION_KEY }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHANNEL_ID: ${{ secrets.TELEGRAM_CHANNEL_ID }}
        run: |
          echo "🚀 Running test_elon_shlomo..."
          echo "Run time: $(date '+%Y-%m-%d %H:%M:%S UTC')"

          # Run the specific test
          if pytest tests/test_elon_shlomo.py::test_elon_shlomo -v; then
            echo "test_passed=true" >> $GITHUB_OUTPUT
            echo "✅ TEST PASSED! Appointment found!"
            exit 0
          else
            echo "test_passed=false" >> $GITHUB_OUTPUT
            echo "❌ Test failed - no appointment found yet"
            exit 1
          fi

      - name: Stop video recording
        if: always()
        run: |
          if [ -f /tmp/ffmpeg_pid ]; then
            FFMPEG_PID=$(cat /tmp/ffmpeg_pid)
            echo "🛑 Stopping video recording (PID: $FFMPEG_PID)"
            kill -INT $FFMPEG_PID || true
            # Wait for ffmpeg to finish encoding
            sleep 3
            echo "✅ Video recording stopped and saved"
          fi

      - name: Report test success
        if: steps.run_test.outputs.test_passed == 'true'
        run: |
          echo "🎉 SUCCESS! The test has passed!"
          echo "An appointment was found and Telegram notification was sent."
          echo "This workflow will now be automatically disabled."

      - name: Auto-disable workflow after success
        if: steps.run_test.outputs.test_passed == 'true'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          echo "🛑 Disabling workflow to stop further scheduled runs..."
          gh workflow disable "test-elon-shlomo-scheduled.yml" -R ${{ github.repository }}
          echo "✅ Workflow disabled successfully!"
          echo "The scheduled runs will stop after this execution."
          echo "You can re-enable it anytime from the Actions tab if needed."

      - name: Report test failure
        if: steps.run_test.outputs.test_passed != 'true'
        run: |
          echo "⏳ Test did not pass this time. Will retry in 5 minutes."
          echo "This workflow will continue checking every 5 minutes."

      - name: Upload test video
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-video-${{ github.run_number }}
          path: videos/*.mp4
          retention-days: 30
          if-no-files-found: warn

      - name: Upload test results (if available)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results-${{ github.run_number }}
          path: |
            *.log
            **/*.log
          retention-days: 7
          if-no-files-found: ignore

  # Job to track how long we've been running
  check-workflow-age:
    runs-on: ubuntu-latest
    if: github.event_name == 'schedule'
    steps:
      - name: Check if 24 hours have passed
        run: |
          echo "⏰ This workflow runs every 5 minutes"
          echo "⚠️ Note: After 24 hours (288 runs), consider disabling this workflow if the test hasn't passed"
          echo "To disable: Go to Actions > Test Elon Shlomo - Every 5 Minutes > '...' menu > Disable workflow"
          echo ""
          echo "Current run number: ${{ github.run_number }}"
          echo "Approximate hours running: $(((${{ github.run_number }} * 5) / 60))"
